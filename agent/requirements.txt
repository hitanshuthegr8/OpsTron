# Range, not a pin: chromadb 1.0.x hard-pins `fastapi==0.115.9`, and Render's
# build image only has manylinux_2_17 chromadb wheels, so pip backtracks into
# that range and an exact `==0.115.6` here makes the resolve impossible.
fastapi>=0.115.9,<0.116
uvicorn[standard]==0.34.0
# supabase 2.31's `realtime` dependency needs pydantic>=2.11.7, so these cannot
# stay exact-pinned without making the whole resolve impossible.
pydantic>=2.11.7,<3
pydantic-settings>=2.7,<3

# LangChain — no version pins, let pip resolve compatible set
langchain
langchain-core
langchain-groq
langchain-google-genai
langchain-text-splitters
google-generativeai

# HTTP
# supabase >=2.31 requires httpx>=0.28; the old 0.27 pin blocks the upgrade.
httpx>=0.28,<0.29
aiohttp==3.11.11
requests==2.32.3

# Database (chromadb import is wrapped in try/except in vector_store.py)
# chromadb must stay >=1.0: 0.4.x pulls chroma-hnswlib, which ships no wheel for
# Python 3.12 on Windows and fails to build without MSVC. 1.x is wheel-only.
numpy
chromadb>=1.0

# Resilience
tenacity

# Integrations
docker==7.1.0
twilio==9.4.0

# Utilities
python-multipart==0.0.20
python-dotenv==1.0.1

# Supabase (Persistent Storage)
# 2.10 validates the API key against a JWT regex and rejects Supabase's current
# `sb_secret_...` keys with a bare "Invalid API key" before any network call.
# 2.31+ accepts both the legacy JWT keys and the new format.
supabase>=2.31,<3
